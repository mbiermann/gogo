<%= form_for ([@app, @app.placements.build]) do |f| %>
  <% if @placement.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@placement.errors.count, "error") %> prohibited
      this placement from being saved:</h2>
    <ul>
    <% @placement.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>